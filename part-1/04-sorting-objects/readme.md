# Класс как структура

* конструктор класса
* поля класса
* методы класса
* параметры по умолчанию
* создание объекта
* массив объектов
* копирование объектов import copy
* из файла в массив объектов
* сортировка объектов
* сортировка вручную
